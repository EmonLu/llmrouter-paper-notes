# 单篇论文记录模板

> 用途：用于系统记录单篇“大模型路由 / LLM Router / Agentic Routing / 推理成本路由策略”论文，方便后续做横向比较、方法复用和研究沉淀。
>
> 使用建议：每读完一篇论文，尽量完整填写；如果暂时没有信息，可以先留空，但建议至少完成“一句话总结、研究问题、方法概览、核心结果、对我的启发”这几部分。

---

# [论文标题]

## 1. 论文基本信息
> 记录论文的元信息，便于快速定位、引用和分类。

- 标题：
- 作者 / 机构：
- 发表时间：
- 会议 / 期刊：
- 论文链接：
- 代码链接：
- 研究方向关键词：
  - 例如：`LLM Routing`、`Cascade`、`Adaptive Inference`、`Cost Optimization`、`Agentic Routing`

## 2. 一句话总结
> 用一句话说明：这篇论文解决了什么问题、用了什么方法、取得了什么结果。

- 总结：

## 3. 研究问题
> 这一部分回答“为什么要做这件事”。

### 3.1 核心问题是什么？
- 

### 3.2 为什么这个问题在大模型路由场景中重要？
- 

### 3.3 该问题主要对应哪些目标？
> 可多选：质量、成本、延迟、可扩展性、鲁棒性、可解释性、在线适应性、部署效率等。

- 目标类型：
- 我的理解：

## 4. 方法概览
> 这一部分回答“它是怎么做的”。

### 4.1 论文提出的方法是什么？
- 

### 4.1.1 算法的核心直觉是什么？
> 用自己的话说清楚：作者到底利用了什么信号、假设或结构，来做出 routing / budget / cascade / workflow 决策。

- 

### 4.1.2 算法按步骤是怎么运行的？
> 尽量写成 step-by-step，而不是一句话带过。建议写到“输入进来后，先做什么、再做什么、最后如何输出决策”。

- Step 1：
- Step 2：
- Step 3：
- Step 4（如果有）：

### 4.1.3 如果把它压缩成一个伪代码 / 决策流，它长什么样？
> 不要求真的写代码，但至少写清楚 decision flow。

- 

### 4.2 Router 的输入是什么？
> 例如：query、历史对话、用户画像、候选模型特征、预算、延迟约束、环境状态等。

- 输入：

### 4.3 Router 的输出是什么？
> 例如：选择哪个模型、是否 fallback、是否 cascade、是否继续推理、分配多少预算等。

- 输出：

### 4.4 Routing 决策是如何产生的？
> 例如：规则、分类器、打分器、排序器、policy、bandit、gating network 等。

- 决策机制：

### 4.5 是否需要训练 Router？
- 是否训练：`是 / 否`
- 如果需要，训练数据是什么：
- 训练目标是什么：

### 4.6 涉及哪些学习机制？
> 可多选：强化学习、监督学习、蒸馏、bandit、ranking、classification、gating、heuristic 等。

- 学习机制：
- 我的理解：

### 4.7 这套算法最依赖什么关键信号？
> 例如：query difficulty、preference label、reward、uncertainty、verifier、历史 memory、profile feature 等。

- 

### 4.8 这套算法最容易失败在哪一步？
> 帮助后续思考真实部署中的 failure mode。

- 

## 5. 系统架构
> 这一部分回答“它在系统里怎么接入”。

### 5.1 整体 Pipeline 是怎样的？
- 

### 5.2 包含哪些模型 / 模块？
- 

### 5.2.1 Router 本身用的是什么模型？
> 重点记清：router 是规则、传统 ML、小模型分类器、奖励模型、policy model，还是直接用 LLM 当 router。

- Router 模型类型：
- Router 模型名称：
- 参数规模 / 大小：
- 是否需要额外训练：
- 我对这个选择的理解：

### 5.2.2 候选大模型池由哪些模型组成？
> 把论文里真正参与 routing / cascade 的模型列出来，而不是笼统写“多个 LLM”。

- 候选模型 A：
  - 类别（开源/闭源、dense/MoE、chat/reasoning/code 等）：
  - 大小 / 参数量：
  - 论文中扮演的角色：
  - 论文里体现出的性能特点：
- 候选模型 B：
  - 类别：
  - 大小 / 参数量：
  - 角色：
  - 性能特点：
- 候选模型 C（如果有）：
  - 类别：
  - 大小 / 参数量：
  - 角色：
  - 性能特点：

### 5.2.3 这些模型之间的能力差异是怎么被利用的？
> 例如：便宜模型负责简单题，强模型负责难题；或 code model / math model / general chat model 各司其职。

- 

### 5.3 路由发生在哪个阶段？
> 例如：请求进入前、生成前、生成中、agent planning 阶段、tool 调用前后等。

- 路由阶段：

### 5.4 是否支持以下能力？
- 动态 fallback：`是 / 否`
- cascade：`是 / 否`
- multi-step decision：`是 / 否`
- online update：`是 / 否`

### 5.5 我对系统架构的理解
- 

### 5.6 如果新增一个候选大模型，router 需要付出什么代价？
> 这是很关键的一栏。重点写：
> - 是不是只要补 profile / metadata 就能接入
> - 还是必须重新收集偏好数据、重新打标签、重新训练 router
> - 成本主要花在离线评测、监督数据、在线探索，还是系统接入工程

- 是否支持低成本新增模型：
- 新增模型时需要做什么：
- 需要重新训练吗：
- 需要重新标注/重新跑 benchmark 吗：
- 我判断的接入成本：低 / 中 / 高
- 原因：

## 6. 实验设置
> 这一部分记录实验是否可信、是否和你的场景相关。

### 6.1 使用了哪些数据集？
- 

### 6.1.1 数据集是怎么来的？
> 很重要：是公开 benchmark、人工标注、用户日志、模型对战数据、judge 合成数据，还是作者自己构造的？

- 数据来源：
- 构造方式：
- 是否有人工标注：
- 是否有模型打标 / judge：
- 我对数据可靠性的判断：

### 6.1.2 数据集里具体包含什么？
> 不要只写名字，要写“样本是什么、标签是什么、输入输出是什么、覆盖哪些任务”。

- 样本形式：
- 输入字段：
- 输出/标签字段：
- 覆盖任务：
- 数据规模：
- 我的理解：

### 6.1.3 这些数据集和真实 router 场景有多接近？
> 判断它到底是在测 toy routing、benchmark routing，还是更接近真实线上流量。

- 

### 6.2 对比了哪些 Baseline？
- 

### 6.3 评估了哪些任务类型？
> 例如：chat、QA、math、code、tool-use、agent tasks、benchmark routing tasks 等。

- 

### 6.4 使用了哪些大模型或专家模型？
- 

### 6.5 主要评估指标是什么？
> 例如：accuracy、win rate、cost、latency、token usage、throughput、success rate 等。

- 

### 6.5.1 每个评估指标分别在衡量什么？
> 不要只列缩写，要解释：这个指标高/低分别意味着什么，对 router 设计有什么约束。

- 指标 A：
  - 衡量含义：
  - 高/低分别意味着：
  - 对 router 设计的启发：
- 指标 B：
  - 衡量含义：
  - 高/低分别意味着：
  - 对 router 设计的启发：

### 6.5.2 这些指标有没有盲点？
> 比如只看 accuracy 不看 cost，只看平均 cost 不看 tail latency，只看 benchmark 不看 online 更新成本。

- 

## 7. 核心结果
> 这一部分只记录最关键结果，不要机械抄整张表。

### 7.1 最重要的实验结果是什么？
- 

### 7.2 相比 Baseline 提升了什么？
- 

### 7.3 是否在质量、成本、延迟之间取得了更好的 trade-off？
- 

### 7.4 有哪些 Ablation Study 或 Sensitivity Analysis？
- 

### 7.5 从这些实验结果里，能看出这个方法真正的优势是什么？
> 这不是重复“结果数值”，而是解释：这些结果说明该方法擅长解决什么问题、在什么条件下特别强。

- 

### 7.6 这些结果说明它更适合哪类场景？
> 比如：
> - 便宜 query-level router
> - 强调低延迟在线服务
> - 适合多阶段 escalation
> - 适合小样本冷启动
> - 适合新增候选模型频繁变化的环境

- 

### 7.7 有哪些结果其实暴露了它的短板？
> 通过负结果、ablation 或某些指标不占优的地方，反推方法边界。

- 

## 8. 贡献与创新点
> 这一部分用于提炼“这篇论文为什么值得记”。

### 8.1 主要贡献是什么？
- 

### 8.2 相比已有方法的新意在哪里？
- 

### 8.3 创新类型属于哪一类？
> 可多选：新的 routing 策略、新的训练目标、新的数据构造方式、新的系统架构、新的 benchmark 等。

- 创新类型：
- 我的判断：

## 9. 局限性
> 这一部分非常重要，用来防止“只看到优点”。

### 9.1 方法有哪些假设？
- 

### 9.2 是否依赖特定模型、数据集或人工标注？
- 

### 9.3 是否存在泛化性、稳定性、成本、延迟、可解释性或部署方面的问题？
- 

### 9.4 作者自己提到的 Limitation 是什么？
- 

### 9.5 我认为还有哪些潜在问题？
- 

## 10. 对我的启发
> 这一部分是把“读论文”变成“服务我自己的研究/工作”。

### 10.1 这篇论文对我理解大模型路由有什么帮助？
- 

### 10.2 有哪些方法可以借鉴？
> 这里不要只写抽象概念，尽量拆成“可以直接借来做系统模块 / 训练流程 / 评估流程”的东西。

- 可直接借鉴的方法点 1：
  - 具体是什么：
  - 可以放到我系统里的哪一层：
  - 为什么值得借：
- 可直接借鉴的方法点 2：
  - 具体是什么：
  - 可以放到我系统里的哪一层：
  - 为什么值得借：

### 10.3 有哪些想法可以扩展？
- 

### 10.4 是否能用于以下场景？
- 企业内部 Copilot：
- LLM 系统路由：
- 多模型选择：
- 成本优化：
- Agent 系统：

### 10.5 这篇论文最值得抄走的，不是结论，而是哪一个“方法部件”？
> 强迫自己回答：如果我只能借一个模块，我借什么？

- 

## 11. 可复现性记录
> 这一部分帮助你判断：这篇论文是“能落地”还是“只能参考思想”。

### 11.1 是否开源代码？
- `是 / 否`
- 链接：

### 11.2 是否开源数据？
- `是 / 否`
- 链接：

### 11.3 关键实现细节是否清楚？
- 

### 11.4 复现难度如何？
> 可用：低 / 中 / 高

- 复现难度：
- 原因：

### 11.5 如果我要复现，第一步应该做什么？
- 

## 12. 横向比较字段
> 这一部分专门为后续表格对比服务，尽量用简短字段填写。

- Routing 对象：
- Routing 粒度：
- Router 类型：
- 是否训练：
- 训练信号：
- 优化目标：
- 支持的模型数量：
- Router 使用的模型：
- Router 模型大小：
- 候选模型池类型：
- 新增模型是否需要重训：
- 新增模型接入成本：低 / 中 / 高
- 是否考虑成本：
- 是否考虑延迟：
- 是否 online：
- 是否开源：
- 主要优点：
- 主要缺点：

## 13. 阅读后的评分
> 建议按 1-5 打分，便于后续快速筛选重点论文。

- 相关性：`1-5`
- 方法新颖性：`1-5`
- 实验可信度：`1-5`
- 工程可落地性：`1-5`
- 对我研究 / 工作的启发：`1-5`

### 总评
- 是否值得精读：`是 / 否`
- 是否值得复现：`是 / 否`
- 是否值得纳入自己的系统设计：`是 / 否`
- 一句话结论：

---

# 多篇论文横向对比表模板

> 用途：当你读完多篇论文后，把每篇论文的重要字段抽出来做横向比较。建议放在 README、survey-notes 或单独的 `comparison.md` 里长期维护。

| 论文 | 年份 | Routing 对象 | Routing 粒度 | Router 类型 | 是否训练 | 训练信号 | 优化目标 | 支持模型数 | 是否考虑成本 | 是否考虑延迟 | 是否 online | 是否开源 | 主要优点 | 主要缺点 |
|---|---:|---|---|---|---|---|---|---:|---|---|---|---|---|---|
| 论文 A | 2024 | 模型选择 | query-level | classifier | 是 | preference / label | quality-cost tradeoff | 4 | 是 | 否 | 否 | 是 | 简单有效 | 泛化有限 |
| 论文 B | 2025 | cascade / fallback | step-level | policy / bandit | 是 | reward | latency-quality tradeoff | 3 | 是 | 是 | 是 | 否 | 动态性强 | 部署复杂 |
| 论文 C | 2026 | agent / workflow routing | trajectory-level | graph policy | 是 | RL | task success + cost | 6 | 是 | 是 | 是 | 部分 | 适合 agent | 训练重 |

## 可选补充字段（如果后面需要更细）
> 如果你后续比较的论文越来越多，可以继续扩展下面这些字段。

- 使用数据集
- 任务类型
- 是否支持 fallback
- 是否支持 cascade
- 是否支持 online learning
- 是否支持多轮 / agent 场景
- 是否考虑 token usage
- 主要 baseline
- 最关键指标提升
- 你的评分
